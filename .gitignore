# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test coverage + tool caches
coverage/
.eslintcache
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
Desktop.ini
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env files (never commit secrets / API keys)
.env
.env.*
!.env.example

# Supabase personal access token for the CAREERS account (deploys). Local-only —
# the machine's global CLI login is a different account that 403s on this project.
.supabase-token

.vercel

# Claude Code per-user local state (permission grants — machine-specific)
.claude/settings.local.json

# Lovable session residue
.lovable/

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# superpowers brainstorming session artifacts (mockups, server state)
.superpowers/
